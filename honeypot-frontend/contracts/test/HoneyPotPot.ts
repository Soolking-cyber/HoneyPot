import { expect } from "chai";

describe("HoneyPotPot", () => {
  it.skip("deploys and seeds the initial pot", async () => {
    // TODO: wire deployment fixture once swap adapter + thirdweb mocks are available.
    expect(true).to.equal(false);
  });
});

